const a=45;
const b=50;
// adding two number datatypes
let sum = a + b;         // Addition
let difference = a - b;  // Subtraction
let product = a * b;     // Multiplication
let quotient = a / b;    // Division
let remainder = a % b;   // Modulus
let power = a ** b;  
// square root
console.log(Math.sqrt(b))
//random numbers
console.log(Math.random()*99999)
//convert to Integer
let randomNumber=Math.random()*99999;
let RandomNumberInt=parseInt(randomNumber);
console.log(RandomNumberInt);